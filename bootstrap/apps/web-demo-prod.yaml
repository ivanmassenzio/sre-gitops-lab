apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: web-demo-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/ivanmassenzio/sre-gitops-lab.git
    targetRevision: HEAD
    path: apps/web-demo/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
